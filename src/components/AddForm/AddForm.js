import React from "react";
import Form from "../Form/Form";

export default function AddForm() {
    return (
        <Form name="add-form" title="Edit task">
            <button className="popup__cancel-button" ></button>
        </Form>
    );
}